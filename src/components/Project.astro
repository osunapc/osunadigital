---
import { Projects } from "../projects";
---

<section class="text-center p-8">
	<h2
		class="block w-full bg-gradient-to-b from-white to-gray-400 bg-clip-text font-bold text-transparent text-3xl sm:text-4xl md:text-5xl"
	>
		Ultimos proyectos
	</h2>
	<main class="flex justify-center items-center flex-col md:flex-row-reverse">
		<div class="w-1/2 p-6">
			<p class="text-gray-400">
				¿Quieres ver lo que puedo hacer por tu negocio? Aquí tienes una
				selección de mis proyectos más recientes, diseñados para generar
				conversiones y destacar tu marca
			</p>
		</div>

		<div
			class="relative h-full flex justify-center items-center w-full md:w-1/2"
		>
			<div class="swiper mySwiperr">
				<div class="swiper-wrapper w-full h-full">
					{
						Projects.map((proyecto) => {
							return (
								<div class="swiper-slide relative">
									<img
										class="object-cover rauded-lg"
										src={proyecto.img}
										alt={proyecto.alt}
									/>
									<div class="absolute bottom-0 mx-auto flex flex-row justify-center gap-4">
										<a
											class="text-white bg-gradient-to-r from-blue-500 via-blue-600 to-blue-700 hover:bg-gradient-to-br focus:ring-4 focus:outline-none focus:ring-blue-300 dark:focus:ring-blue-800 shadow-lg shadow-blue-500/50 dark:shadow-lg dark:shadow-blue-800/80 font-medium rounded-lg text-sm px-5 py-2.5 text-center me-2 mb-2"
											href={proyecto.github}
											target="_blank"
										>
											github
										</a>

										<a
											class="text-white bg-gradient-to-r from-blue-500 via-blue-600 to-blue-700 hover:bg-gradient-to-br focus:ring-4 focus:outline-none focus:ring-blue-300 dark:focus:ring-blue-800 shadow-lg shadow-blue-500/50 dark:shadow-lg dark:shadow-blue-800/80 font-medium rounded-lg text-sm px-5 py-2.5 text-center me-2 mb-2"
											href={proyecto.link}
											target="_blank"
										>
											Link
										</a>
									</div>
								</div>
							);
						})
					}
				</div>
			</div>
		</div>
	</main>
</section>
<style>
	.swiper {
		width: 240px;
		height: 320px;
	}

	.swiper-slide {
		display: flex;
		align-items: center;
		justify-content: center;
		border-radius: 18px;
		font-size: 22px;
		font-weight: bold;
		color: #fff;
	}
</style>

<script>
	import "swiper/css";
	import Swiper from "swiper";
	import { register } from "swiper/element/bundle";
	register();

	var swiper = new Swiper(".mySwiperr", {
		effect: "cards",
		grabCursor: true,
	});
</script>
